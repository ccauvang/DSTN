**Plan to develop order and login, create account feature**

first is need to create a back-end server to handle API call from font-end

next crate login and crate account feature for user

when user buy anything on the site require them to give us more info like name location to get the ship phone number collected all that send to back-end and store them into data base to next time buying they no need to retyping that info

then in the back-end send noti to staff so they can connect to customer and re-check the info and make sure the order is real.
